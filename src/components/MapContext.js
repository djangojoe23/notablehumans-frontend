// MapContext.js
//This context will hold your Mapbox map instance so that other components (like MarkerComponent) can access it.

import React from 'react';

export const MapContext = React.createContext(null);
